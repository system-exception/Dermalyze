# =============================================================================
# Skin Lesion Classification Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Data Configuration
# -----------------------------------------------------------------------------
data:
  images_dir: data/HAM10000_Training/images
  labels_csv: data/HAM10000_Training/labels_with_metadata.csv
  split_seed: 42
  val_size: 0.15
  test_size: 0.15
  lesion_aware: true
  use_stratified_group_kfold: true
  kfold:
    n_splits: 5
    fold_index: 0
    group_column: lesion_id

  # Metadata Configuration (Optional)
  # Enable to use patient metadata (age, sex, anatomical site) along with images
  # Requires CSV to have columns: age, sex, localization
  use_metadata: true
  metadata_columns: [age_approx, anatom_site, sex]

  # Segmentation-Assisted ROI Crop (Optional)
  segmentation:
    enabled: true
    masks_dir: data/HAM10000_Segmentations
    required: true
    mask_threshold: 10
    crop_margin: 0.10
    filename_suffixes: ["", "_segmentation", "_mask"]

# -----------------------------------------------------------------------------
# Model Configuration
# -----------------------------------------------------------------------------
model:
  backbone: efficientnetv2_s
  num_classes: 7
  image_size: 224
  pretrained: true
  dropout_rate: 0.3
  freeze_backbone: false

# -----------------------------------------------------------------------------
# Training Configuration
# -----------------------------------------------------------------------------
training:
  seed: 42
  batch_size: 16
  epochs: 40
  lr: 0.00025
  weight_decay: 0.02

  # Two-Stage Training
  stage1_epochs: 5
  stage1_lr: 0.001
  stage1_weight_decay: 0.01
  stage2_epochs: 35
  stage_epoch_mode: fit_total
  stage2_lr: 0.0005
  stage2_weight_decay: 0.02

  # Data Loading
  num_workers: 4
  prefetch_factor: 2
  persistent_workers: true
  use_weighted_sampling: true
  sampling_weight_power: 0.35
  sampling_weight_min: 0.5
  sampling_weight_max: 5.0
  augmentation: light

  # Performance Optimizations
  use_amp: true
  use_gradient_checkpointing: true
  gradient_accumulation_steps: 2

  # Data Augmentation
  mixup_alpha: 0.0
  cutmix_alpha: 0.0
  mixup_prob: 0.0
  cutmix_prob: 0.0

  # Learning Rate Scheduler
  scheduler:
    type: cosine
    # warmup_pct: 0.05
    # Cosine scheduler options (if type: cosine):
    T_0: 20
    T_mult: 1
    eta_min: 0.00001

  # Exponential Moving Average
  ema:
    enabled: true
    decay: 0.999
    use_for_eval: true
    save_best: false

  # Early Stopping
  early_stopping_patience: 7

# -----------------------------------------------------------------------------
# Loss Function
# -----------------------------------------------------------------------------
loss:
  type: focal
  alpha: null
  label_smoothing: 0.03
  class_weight_power: 0.0

# -----------------------------------------------------------------------------
# Output Configuration
# -----------------------------------------------------------------------------
output:
  save_epoch_checkpoints: false

# -----------------------------------------------------------------------------
# Evaluation Configuration
# -----------------------------------------------------------------------------
evaluation:
  tta:
    use_tta: true
    mode: full
    aggregation: mean
    use_clahe_tta: true
    clahe_clip_limit: 2.0
    clahe_grid_size: 8
